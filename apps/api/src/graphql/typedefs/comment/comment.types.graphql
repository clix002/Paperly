type Comment {
  id: ID!
  content: String!
  documentId: ID!
  authorId: ID!
  parentId: ID
  isAi: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
  author: User
  document: Document
}

type CommentPagination {
  docs: [Comment!]!
  info: Pagination!
}
